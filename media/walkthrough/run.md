The game runs on the Pyxel Web runtime in a panel beside your code, so nothing
is installed locally. The runtime is loaded from jsDelivr, which needs an
internet connection.

Saving a file in the game's folder reloads it while `pyxel.autoReload` is on.
Screenshots and screencasts you capture in the game are saved next to the
file you opened.
