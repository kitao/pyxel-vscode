The examples match the Pyxel version bundled with this extension and land in
a `pyxel_examples` folder inside the folder you pick.

Start with `01_hello_pyxel.py`, then read `02_jump_game.py` and
`10_platformer.py` to see how a complete game is structured. Each example is
a single file you can copy into your own project.
