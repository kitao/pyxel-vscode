Sprites, tilemaps, sounds, and music live in `.pyxres` files. Saving in the
Pyxel Editor writes the file back to your workspace, and a game running from
the same folder reloads while `pyxel.autoReload` is on.

`.pyxapp` packages open in the Pyxel Player the same way.
